l0COA ( ID ) {
    % Level 1 COA entries
    % For each l1coa in L1COAS
    l1COA ( ID ) {
        % Include total_cost if present
        % If TOTAL_COST_VALUE and TOTAL_COST_UNIT are provided
        total_cost {
            value = TOTAL_COST_VALUE
            unit = TOTAL_COST_UNIT
        }
        % Level 2 COA entries
        % For each l2coa in L2COAS
        % Include L2COA template
    }
}
