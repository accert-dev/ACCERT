l3coa: l3COA ( ID ) {
    % Include total_cost if present
    total_cost {
        value = VALUE
        unit = UNIT
    }
    % Cost elements (ce)
    % For each ce in CES
    ce ( ID ) {
        % Include algorithm
    }
    % New COA entries
    % For each newcoa in NEWCOAS
    newCOA ( ID ) {
        descr = "DESCR"
    }
}
